# Hoist drizzle-orm to prevent multiple instances
# This ensures type compatibility across the monorepo
public-hoist-pattern[]=*drizzle-orm*
public-hoist-pattern[]=*drizzle-kit*

# Enable strict peer dependencies to catch version conflicts
strict-peer-dependencies=false

# Shamefully hoist to ensure single instance
shamefully-hoist=true
