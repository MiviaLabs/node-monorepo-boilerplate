module.exports = {
  // Security rules - disabled due to ESLint 8 compatibility issues with plugin naming
  // TODO: Re-enable when upgrading to ESLint 9+
  rules: {
    // Placeholder for future security rules
  }
};
