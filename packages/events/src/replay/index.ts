/**
 * @module EventsReplay
 * @description Event replay service for reprocessing failed or historical events.
 */

export { EventReplayService, ReplayStatus } from './event-replay.service';
export type { ReplayOptions, ReplaySession, ReplayEventResult } from './event-replay.service';
